# 修改记录

## 记录规则

- 每次用户提出调整需求后，都要把需求和实际完成的改动记录到本文件。
- 每条记录尽量写清时间、调整内容和落地结果，便于回看。

## 记录

### 2026-06-30

- 调整需求：将根目录的说明文件从 `README.md` 改为 `AGENTS.md`，并让后续协作规则写入 `AGENTS.md`。
- 完成结果：已将根目录 `README.md` 改名为 `AGENTS.md`，原内容保留。

### 2026-06-30

- 调整需求：创建 `纠错记录.md` 和 `修改记录.md`，并把“先看纠错记录再执行”“用户纠错要记入纠错记录”“每次调整要记入修改记录”这几条写入 `AGENTS.md`。
- 完成结果：已创建两个记录文件，并已把对应规则补充进 `AGENTS.md`。

### 2026-06-30

- 调整需求：按页面反馈调整组织管理页，包括缩短操作列、移除“调整排序”按钮、删除顶部启用/停用标签栏，并新增默认值为“启用”的状态查询字段。
- 完成结果：已更新组织管理页配置与通用列表展示逻辑，并同步调整相关检查脚本；页面已确认移除顶部状态标签和“调整排序”按钮，新增了默认“启用”的状态查询项，重置后也会回到“启用”。

### 2026-06-30

- 调整需求：在组织管理页原型调整后，同步检查 `docs/OA_PRD.md` 是否需要更新，避免文档和页面不一致。
- 完成结果：已同步更新 `docs/OA_PRD.md` 中组织列表、组织主数据、页面要求和组织管理页面说明，去掉“调整排序按钮”口径，改为通过排序号维护顺序，并补充状态查询字段默认“启用”的说明。

### 2026-06-30

- 调整需求：组织管理页当状态筛选默认为“启用”时，列表也要直接显示对应筛选结果，不能只显示默认值不生效。
- 完成结果：已补上列表查询过滤逻辑，组织管理页进入时会自动按默认“启用”过滤；页面已确认状态显示为“启用”，列表仅展示启用数据，共 19 条；同时已回看 `docs/OA_PRD.md`，本次无需新增文档改动。

### 2026-06-30

- 调整需求：岗位管理参考组织管理的调整方式同步修改。
- 完成结果：已将岗位管理页顶部启用/停用标签移除，查询区新增默认“启用”的状态筛选，并让列表进入页面时直接按该条件过滤；页面已确认仅展示启用岗位，共 19 条；同时已同步更新 `docs/OA_PRD.md` 中岗位管理页面说明。

### 2026-06-30

- 调整需求：员工信息详情页中的多条数据改为“编辑后添加一条表单”的样式，工作经历参考图片展示，点击编辑后可取消或保存，编辑状态下显示“添加工作经历”按钮，点击后向下新增一条填写表单。
- 完成结果：已将员工详情页的工作经历、教育经历、家庭成员改为多条表单样式；工作经历字段已调整为公司名称、担任职位、入职日期、离职日期、职位描述；页面已验证工作经历可进入编辑、取消/保存按钮可见、点击添加后新增一条表单；并已同步更新 `docs/OA_PRD.md` 中员工档案相关说明。

### 2026-06-30

- 调整需求：根据 `docs/OA_PRD.md` 检查并修正员工信息详情页编辑态字段，字段类型、是否必填、是否可编辑和选择项不能都用普通文本框代替。
- 完成结果：已将员工详情页基础信息、个人信息、银行卡信息和多条档案记录的编辑控件按 PRD 调整为下拉、日期、数字、开关、上传、文本域或只读展示；页面已验证所属部门、岗位、证件类型、民族、学历等为下拉，入职日期和档案日期为日期选择，试用期（月）为数字输入，是否试用期为开关，工号、司龄等只读字段不会变成输入框；本次是按现有 PRD 落地，PRD 无需新增改动。

### 2026-06-30

- 调整需求：梳理员工信息与合同与协议之间的合同字段同步方案；明确员工信息合同字段保持原状不新增，合同与协议中的开始日期、结束日期、签订日期需改名为合同开始日期、合同结束日期、合同签订日期，并删除试用期字段。
- 完成结果：已同步调整 PRD 和原型页面；员工信息仅保留合同公司、合同开始日期、合同结束日期三个合同字段且可编辑；合同与协议页面已将开始日期、结束日期、签订日期改为合同开始日期、合同结束日期、合同签订日期，并删除试用期结束日期；已补充员工信息与合同与协议之间的字段同步逻辑，合同新增、编辑、解除、终止会回写员工合同字段，员工详情保存合同字段会同步合同记录。

### 2026-06-30

- 调整需求：按页面反馈调整员工信息详情页，头像上传入口改到顶部头像悬停处，基础信息不再展示员工头像；年龄字段放在出生日期后；民族下拉选择项补完整。
- 完成结果：已完成原型和 PRD 同步调整；顶部头像悬停显示上传头像入口，基础信息已移除员工头像字段，年龄已紧跟出生日期展示，民族下拉已补充完整民族选项；已新增检查脚本防止后续回退。

### 2026-06-30

- 调整需求：员工信息详情页中“离职原因”和“离职日期”不可编辑。
- 完成结果：已将员工详情页基础信息里的离职日期和离职原因设置为只读；PRD 已是离职流程驱动口径，本次无需新增 PRD 改动；已补充检查脚本防止后续回退。

### 2026-06-30

- 调整需求：新增员工页面按详情页字段调整；新增员工不展示工号、员工状态、司龄、工龄、离职日期、离职原因、年龄；教育经历、工作经历、家庭成员、附件存档需要在新增员工时按表单块填写。
- 完成结果：已移除新增员工表单中的系统生成和流程结果字段，并将教育经历、工作经历、家庭成员、附件存档改为“+ 添加…”表单块；点击添加后会在当前块下展开一条填写表单；已同步更新 PRD 和检查脚本。

### 2026-06-30

- 调整需求：继续优化新增员工抽屉，包括“员工主档”改为“基本信息”、压缩证件照正反面上传区域、隐藏“是否试用期”、统一教育经历/工作经历/家庭成员/附件存档标题样式、上移删除按钮避免压住输入框，并将底部按钮改为取消和保存。
- 完成结果：已完成原型、PRD 和检查脚本同步调整；新增员工底部仅保留取消和保存，取消关闭抽屉，保存执行保存；档案块标题样式已与普通分组一致，删除按钮已上移。

### 2026-06-30

- 调整需求：员工详情页的附件存档也要按照表单块形式展示；新增员工表单块删除按钮改为与员工详情页一致的垃圾箱图标按钮；新增员工附件上传改为与详情页一致的样式。
- 完成结果：已将员工详情页附件存档纳入表单块展示；新增员工表单块删除按钮已改为垃圾箱图标；新增员工附件上传已改为小上传按钮样式；已同步更新 PRD、纠错记录和检查脚本。

### 2026-06-30

- 调整需求：新增员工的证件照正面与证件照反面两个字段也要改下样式。
- 完成结果：已将新增员工里的证件照正面、证件照反面改为与详情页一致的小上传按钮样式，不再显示大面积拖拽上传框；已同步更新 PRD、纠错记录和检查脚本。

### 2026-07-01

- 调整需求：分别输出一份原型和 PRD 的 html 文件，方便直接打开查看。
- 完成结果：已新增导出脚本，并生成两份单独可打开的 html 文件：一份为原型单文件版，一份为 PRD 单文件版；同时保留现有 PRD 与原型内容不变，仅补充导出能力。

### 2026-07-01

- 调整需求：PRD 的模块需求目录需要更清晰，像人力资源模块中的组织管理、员工管理这样的二级菜单也要列出来。
- 完成结果：已调整 PRD html 的目录生成规则；目录现在会在“模块需求”下展开更细一层的菜单，并补充组织管理、员工信息、岗位管理等具体入口，显示文案也做了适当收短，便于快速定位。

### 2026-07-01

- 调整需求：PRD 导出的 html 中流程图不能正常显示，需要能直接看到图形。
- 完成结果：已补上 PRD html 的流程图渲染能力；导出脚本现在会识别文档中的流程图内容并在 html 中直接渲染显示，同时补充离线可用的渲染依赖，重新导出后的文件已实际验证能生成图形。

### 2026-07-01

- 调整需求：启动当前原型项目服务，确保页面可以访问。
- 完成结果：已重新启动开发服务，并实际回查确认页面可打开；当前可通过 `http://localhost:5173/` 访问。

### 2026-07-01

- 调整需求：员工信息的新增页面和详情页面中，合同公司取组织管理里类型为公司的数据；是否试用期、转正日期不可编辑。
- 完成结果：已将合同公司改为从组织类型为“公司”且启用的数据中取值；员工新增页不展示是否试用期和转正日期，员工详情页中这两个字段展示为只读；已同步更新 PRD 和纠错记录。

### 2026-07-01

- 调整需求：员工档案页“新增工作经历”按钮点击后要有实际交互。
- 完成结果：已将员工档案页各页签的新增按钮改为打开对应新增表单；保存后新记录会加入当前页签列表；已同步更新 PRD、纠错记录和检查脚本。

### 2026-07-01

- 调整需求：员工状态增加“试用期”；试用期结束日期大于当天时员工状态为试用期，试用期结束日期小于等于当天或转正日期小于等于当天时转为在职；试用期（月）改为非负整数并放在试用期结束日期之前；试用期结束日期按入职日期加试用期（月）计算；删除是否试用期字段；本次不生成 HTML 文件。
- 完成结果：已同步调整员工新增页、员工详情页、员工列表和花名册筛选、员工状态判断规则和 PRD；试用期（月）限制为非负整数，试用期结束日期自动计算且不可编辑，新增和详情均不再展示是否试用期字段；本次按要求未执行 HTML 导出。

### 2026-07-01

- 调整需求：各表单选择员工时，下拉选项需要展示为“姓名(工号) 部门名称”；各表单选择部门时，下拉选项需要展示为树状；员工信息新增页和详情页中的试用期结束日期需要可编辑，既可按试用期（月）自动计算，也可手工修改。
- 完成结果：已同步调整原型和 `docs/OA_PRD.md`；员工相关下拉已统一展示姓名、工号和部门，部门相关下拉已改为树状选择；员工信息新增页和详情页中的试用期结束日期已支持自动带出和手工修改两种方式，并已实际打开页面检查交互，重新运行测试和构建通过。

### 2026-07-01

- 调整需求：工龄、司龄改为保留一位小数并四舍五入；员工选择器在下拉中显示“姓名(工号) 部门名称”，但选中后表单中仍只显示姓名；合同与协议表单删除“提醒天数”字段，提醒规则固定为提前 30 天提醒。
- 完成结果：已同步调整原型和 `docs/OA_PRD.md`；员工详情里的工龄、司龄已改为保留 1 位小数显示；员工选择器已改为下拉显示完整信息、选中后表单只显示姓名；合同与协议表单已删除提醒天数字段，并按固定提前 30 天提醒处理；已重新跑检查并实际打开页面验证。

### 2026-07-02

- 调整需求：参考用户提供的费控思维导图，先分析费控模块并只调整 `docs/OA_PRD.md`；待 PRD 确认后再修改原型。费控管理收口为预算管理、费用申请、费用报销、发票夹、费用类型五个功能。
- 完成结果：已先完成 PRD 调整，统一费控模块范围与页面目录，补充费用申请付款方式规则、个人垫款与费用报销关系、发票夹与费用报销的引用关系，并删除费控中的“费用统计”范围；同时已核对当前原型菜单仍存在旧费控入口，但按本次要求暂未修改原型，等待 PRD 确认后再同步页面。

### 2026-07-02

- 调整需求：将本次费控 PRD 调整同步到原型；同时补充员工信息规则，新增员工时显示工号字段，工号自动编号且不可编辑。
- 完成结果：已同步调整费控原型菜单和页面内容，费控管理现仅保留预算管理、费用申请、费用报销、发票夹、费用类型五个入口，并按新口径更新了付款方式、报销引用发票夹、发票识别/查验、费用分组等页面内容；同时已将新增员工页面改为展示只读工号并自动带出编号，PRD、原型和纠错记录已同步更新。
